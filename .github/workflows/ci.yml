name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
  workflow_dispatch:
    inputs:
      update_visual_baselines:
        description: 'Re-capture Linux visual baselines and upload as artifact (do not enable on regular runs)'
        type: boolean
        default: false

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '24'

jobs:
  typecheck-lint:
    name: Type-check & lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - run: npm ci

      - run: npm run type-check

      - run: npm run lint

      - name: Version sync (package.json ↔ CHANGELOG)
        run: bash scripts/check-version-sync.sh

      # Syntax-check JS that doesn't flow through the TS/Next.js pipeline.
      # Caught nothing today, but had this been in place yesterday it would
      # have caught the duplicate `const windowUniques` in traffic/index.html
      # before merging — the bug that left the live page stuck on "Loading…"
      # for ~12 hours (PR #88).
      - name: Syntax check (standalone JS + inline HTML scripts)
        run: |
          # Standalone scripts under scripts/
          find scripts -maxdepth 2 -type f \( -name '*.js' -o -name '*.mjs' \) \
            -not -name 'check-inline-js.sh' \
            -print0 | xargs -0 -n1 node --check
          # Inline <script> blocks in static HTML
          bash scripts/check-inline-js.sh \
            traffic/index.html \
            docs/traffic/index.html

  unit:
    name: Unit tests (jest)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      # Some module-level imports read these — locally a `.env` file supplies
      # them; in CI we provide throwaway values so import-time crashes don't
      # take out the whole worker.
      SESSION_SECRET: ci_test_session_secret_padding32
      PIN_ENCRYPTION_KEY: ci_test_pin_encryption_key_pad32
      ENCRYPTION_KEY: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - run: npm ci

      # Integration tests (src/lib/db/__tests__/integration.test.ts) require
      # a real Postgres + seeded schema. They self-skip when E2E_HAS_TEST_DB
      # is not '1' (this job doesn't set it), so a plain jest run is enough.
      - run: npm test -- --ci

  mcp:
    name: MCP server (build + smoke)
    runs-on: ubuntu-latest
    timeout-minutes: 10

    # The MCP server carries its own manifest and lockfile under /.mcp, and
    # nothing else in CI looked at it. Dependabot has been opening PRs against
    # that lockfile all along, and they came back green off the root project's
    # checks alone — including a major, where the only thing that actually
    # verified the server still ran was someone building it by hand. Now that
    # patch and minor updates auto-merge, that had to stop being a manual step.
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: .mcp/package-lock.json

      - run: npm ci
        working-directory: .mcp

      - run: npm run build
        working-directory: .mcp

      # Starts the built server over stdio and speaks MCP to it: initialize,
      # then tools/list, then checks the expected tools are all present. Catches
      # a dependency that breaks the SDK handshake, which a type-check cannot.
      - run: npm run smoke
        working-directory: .mcp

  migration-replay:
    name: Migration replay (modality TODO #3)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies (migrate.js needs the postgres module)
        run: npm ci

      - name: Run migration replay script
        run: bash scripts/test-migration-replay.sh

  playwright-image:
    name: Resolve Playwright image tag
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.resolve.outputs.tag }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      # The container image and @playwright/test must move together, or the
      # browser binaries in the image do not match the client and every spec
      # fails with "Executable doesn't exist at /ms-playwright/...". Deriving
      # the tag from package.json means Dependabot's bump carries the image
      # with it: Dependabot cannot edit a workflow `container:` field, so a
      # hardcoded tag had to be fixed by hand on every Playwright bump.
      - id: resolve
        run: |
          version=$(node -p "require('./package.json').devDependencies['@playwright/test'].replace(/^[^0-9]*/, '')")
          echo "tag=v${version}-jammy" >> "$GITHUB_OUTPUT"
          echo "Playwright image: mcr.microsoft.com/playwright:v${version}-jammy"

  e2e-modality:
    name: E2E modality suite (reverse-proxy)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: playwright-image

    # Use the official Playwright container so Chromium is preinstalled.
    # Previously the job downloaded Chromium from cdn.playwright.dev at
    # job start, which began hanging indefinitely on GH-hosted runners
    # around 2026-05-29 and killed the job at the timeout. The tag comes
    # from @playwright/test in package.json via the playwright-image job,
    # so the two cannot drift apart.
    container:
      image: mcr.microsoft.com/playwright:${{ needs.playwright-image.outputs.tag }}

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: prism
          POSTGRES_PASSWORD: ci_test_password
          POSTGRES_DB: prism
        options: >-
          --health-cmd "pg_isready -U prism"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      # Inside container jobs services are reached by their alias on the
      # docker network, not via localhost port forwards.
      DATABASE_URL: postgresql://prism:ci_test_password@postgres:5432/prism
      REDIS_URL: redis://redis:6379
      PORT: '3000'
      APP_URL: http://localhost:3000
      # Throwaway test-only values — the CI runner is ephemeral and never
      # accepts external connections. Real production secrets live in .env.
      SESSION_SECRET: ci_test_session_secret_padding32
      PIN_ENCRYPTION_KEY: ci_test_pin_encryption_key_pad32
      ENCRYPTION_KEY: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
      # Gate flags consumed by playwright specs that are unsafe to run against
      # a real deployment (would lock out the parent account on PIN failure).
      E2E_HAS_TEST_DB: '1'
      E2E_PIN: '1234'

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - run: npm ci

      - name: Install psql (not bundled in Playwright image)
        # Drop the NodeSource apt source first — it periodically 403s and aborts
        # `apt-get update` (Node is already in the image; we only need psql from
        # the Ubuntu repos here).
        run: rm -f /etc/apt/sources.list.d/nodesource* && apt-get update && apt-get install -y --no-install-recommends postgresql-client

      - name: Apply base schema
        env:
          PGPASSWORD: ci_test_password
        run: psql -h postgres -U prism -d prism -v ON_ERROR_STOP=1 -f src/lib/db/init/02-schema.sql

      - name: Apply migrations
        run: node scripts/migrate.js

      - name: Seed database
        run: npm run db:seed

      - name: Run e2e modality suite
        run: npx playwright test e2e/reverse-proxy.spec.ts e2e/mobile-pwa-settings.spec.ts --reporter=list

      # Geometry, separately: every calendar bug in the month grid has been a
      # rendered box in the wrong place, which no unit test can see. The classes
      # and props were correct each time.
      - name: Run calendar geometry suite
        run: npx playwright test e2e/calendar-geometry.spec.ts --reporter=list

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  visual-regression:
    name: Visual regression (modality TODO #2)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    needs: playwright-image

    # See e2e-modality for why we run inside the Playwright image.
    container:
      image: mcr.microsoft.com/playwright:${{ needs.playwright-image.outputs.tag }}

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: prism
          POSTGRES_PASSWORD: ci_test_password
          POSTGRES_DB: prism
        options: >-
          --health-cmd "pg_isready -U prism"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://prism:ci_test_password@postgres:5432/prism
      REDIS_URL: redis://redis:6379
      PORT: '3000'
      APP_URL: http://localhost:3000
      SESSION_SECRET: ci_test_session_secret_padding32
      PIN_ENCRYPTION_KEY: ci_test_pin_encryption_key_pad32
      ENCRYPTION_KEY: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
      E2E_HAS_TEST_DB: '1'
      E2E_PIN: '1234'
      # Freeze what "today" means for this job, for the seed and the browser
      # alike. The fixtures are built relative to now and the calendar draws
      # itself around today, so without this a baseline captured on one date
      # cannot match a run on the next, and the calendar half of the suite went
      # red for every pull request after the first midnight UTC. A Wednesday
      # mid-month, so month grids show a full picture and nothing sits on a
      # month or week boundary.
      PRISM_SEED_NOW: '2026-06-17T09:30:00Z'

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - run: npm ci

      - name: Install psql + redis-cli (not bundled in Playwright image)
        # Drop the NodeSource apt source first — it periodically 403s and aborts
        # `apt-get update` (Node is already in the image; we only need the
        # Postgres/Redis clients from the Ubuntu repos here).
        run: rm -f /etc/apt/sources.list.d/nodesource* && apt-get update && apt-get install -y --no-install-recommends postgresql-client redis-tools

      - name: Apply base schema
        env:
          PGPASSWORD: ci_test_password
        run: psql -h postgres -U prism -d prism -v ON_ERROR_STOP=1 -f src/lib/db/init/02-schema.sql

      - name: Apply migrations
        run: node scripts/migrate.js

      - name: Seed database
        run: npm run db:seed

      # Detect whether Linux baselines have been captured yet. Until they
      # are, the compare run would fail every push because Playwright reports
      # "missing baseline" as a failure. The bootstrap flow is: trigger this
      # workflow with update_visual_baselines=true, download the artifact,
      # commit the baselines, push. From then on the compare runs as a gate.
      - name: Check for Linux baselines
        id: check-baselines
        run: |
          if ls e2e/visual-regression.spec.ts-snapshots/*-chromium-linux.png > /dev/null 2>&1; then
            echo "found=true" >> $GITHUB_OUTPUT
          else
            echo "found=false" >> $GITHUB_OUTPUT
          fi

      # Three execution modes:
      #   - Default + baselines exist: compare, fail on diff.
      #   - Default + no baselines: skip (advisory only, prints reminder).
      #   - workflow_dispatch with update_visual_baselines=true: regenerate
      #     Linux baselines and upload them as an artifact for download +
      #     manual commit.
      - name: Run visual regression suite (compare)
        if: ${{ steps.check-baselines.outputs.found == 'true' && (github.event_name != 'workflow_dispatch' || inputs.update_visual_baselines != true) }}
        # list AND html: `--reporter=list` alone replaces the config's html
        # reporter, so playwright-report/ was empty and the upload below
        # produced no artifact. A failing run said which shots differed and
        # nothing about how, which is the one thing a pixel diff is for.
        run: npx playwright test e2e/visual-regression.spec.ts --reporter=list,html

      # Fail, do not skip. This is a required check: for months it reported
      # success while comparing nothing, because no Linux baselines existed and
      # the missing-baseline path exited 0. A gate that passes when it cannot
      # run is worse than no gate, since it also reports that it ran.
      - name: Fail when Linux baselines are missing
        if: ${{ steps.check-baselines.outputs.found == 'false' && (github.event_name != 'workflow_dispatch' || inputs.update_visual_baselines != true) }}
        run: |
          echo "::error::No Linux visual baselines are committed, so nothing was compared."
          echo "::error::Regenerate them: run this workflow manually with update_visual_baselines=true,"
          echo "::error::download the visual-regression-linux-baselines artifact, and commit it."
          exit 1

      # `--update-snapshots` alone means "changed": it runs the comparator and
      # only rewrites a baseline whose diff exceeds maxDiffPixelRatio. With the
      # ratio at 1% (20,736px of a 1920x1080 frame) a small but wrong region
      # survives regeneration untouched, which is how the dev-overlay badge
      # stayed in most baselines through a full re-capture. `=all` rewrites
      # unconditionally, which is what a deliberate re-capture should mean.
      - name: Run visual regression suite (update baselines)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.update_visual_baselines == true }}
        run: npx playwright test e2e/visual-regression.spec.ts --update-snapshots=all --reporter=list

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: visual-regression-report
          # test-results/ carries the actual/expected/diff PNGs for each failed
          # comparison. The html report links to them but does not contain them.
          path: |
            playwright-report/
            test-results/
          retention-days: 7

      # always(): Playwright exits non-zero when it writes a snapshot that did
      # not exist, which is exactly what a baseline-generation run does. Without
      # this the job stops at that failure and the baselines it just produced
      # are thrown away, so the documented bootstrap path could never complete.
      - name: Upload regenerated Linux baselines
        if: ${{ always() && github.event_name == 'workflow_dispatch' && inputs.update_visual_baselines == true }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4
        with:
          name: visual-regression-linux-baselines
          path: e2e/visual-regression.spec.ts-snapshots/*-chromium-linux.png
          retention-days: 14
