# Secrets
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
!.env.example

# Wrangler local secrets (telemetry collector). Keep tokens out of git.
.dev.vars
.claude/settings.local.json

# Per-machine git hooks. .husky/pre-commit and friends call these when they
# exist, so a checkout can add its own checks without the repository carrying
# them for everyone.
.husky/local/

# Dependencies
node_modules/

# Build outputs
.next/
out/
*.tsbuildinfo
.next/standalone/
# MkDocs build output
site/

# DB script bundles (rebuilt on every Docker build)
dist/

# Generated by next-pwa at build time
public/sw.js
public/sw.js.map
public/workbox-*.js
public/workbox-*.js.map

# Runtime & cache
.npm
.pnpm-store/
.cache/
/cache/
uploads/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
logs/
*.log

# Test outputs
coverage/
.nyc_output/
test-results/
playwright-report/
playwright/.cache/
e2e/demos/videos/

# OS files
.DS_Store
.AppleDouble
.LSOverride
._*
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
*~

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
.idea/
*.iml
*.ipr
*.iws
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~
\#*\#
.#*

# Docker
docker-data/
data/
uploads/
/backups/
config/rclone.conf
config/certs/

# Misc
.vercel

# Alexa Skill Kit CLI (LWA refresh tokens, build state, deploy artifacts)
.ask/
ask-resources.json
.deploy/
*.pem
*.key
*.p12
*.pfx
drizzle/meta/
*.db
*.sqlite
*.sqlite3
tmp/
temp/
*.tmp
*.temp
screenshots/
*.bak
*.backup
*.zip
*.tar.gz
*.rar
# Claude Code: ignore runtime state, keep shared config + hooks tracked
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/hooks/**
docs/MARKETING.md
docker-compose.override.yml

# Internal dev docs (keep locally, not tracked)
CLAUDE.md
LESSONS_LEARNED.md
IMPLEMENTATION_ROADMAP.md
DOCKER_ADMIN.md
prism-requirements_v19.md
docs/requirements/
docs/REQUIREMENTS.md
docs/SIDE_NAVIGATION.md
nul

# Claude Code artifacts
transcript*.json

# Lighthouse reports
lighthouse-*
/public/twemoji/
/public/maplibre/

# Python bytecode from scripts/
__pycache__/

# A local-only config file that must never be committed. Named here because
# the cost of committing one by accident is higher than the cost of a line.
prism-pii-denylist.txt

# UI-audit output. board.html base64-inlines every screenshot into one
# self-contained file, so a local run against a real instance would otherwise
# leave a committable artifact no text scanner can read.
e2e/ui-audit-artifacts/
